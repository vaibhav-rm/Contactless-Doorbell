server.port=8080

# PostgreSQL connection settings
spring.datasource.url=jdbc:postgresql://localhost:5433/doorbell_db
spring.datasource.username=doorbell
spring.datasource.password=doorbell_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Telegram Bot Integration
telegram.bot.token=8761369663:AAE7vcLQ4UESOMBTC7sC_EfWjx0HZkqaWyY
telegram.chat.id=1239023229

# If Telegram is blocked (ban) in your country, uncomment and set these:
# Optional: Use a local HTTP/SOCKS VPN Proxy (e.g. Clash, V2ray, shadowsocks running on Pi/PC)
# telegram.proxy.host=127.0.0.1
# telegram.proxy.port=7890

# Optional: Use a custom Telegram API mirror/reverse proxy URL
# telegram.api.url=https://api.telegram.org
